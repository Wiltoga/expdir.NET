expdir()
{
    dir="<install directory>"
    "${dir}/expdirapp" "$@"
    if test -f "${dir}/location"; then
        location="$(cat ${dir}/location)"
        cd "${location}"
        rm "${dir}/location"
    fi
}
